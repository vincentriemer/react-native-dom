{
  "name": "rnpm-plugin-dom",
  "version": "0.0.10",
  "description": "rnpm plugin that generates a DOM template project",
  "main": "index.js",
  "keywords": ["rnpm", "react-native", "react-native-dom", "rnpm dom"],
  "engines": {
    "node": ">= 7.0.0"
  },
  "scripts": {
    "compile": "shx echo 'no compilation needed'"
  },
  "license": "MIT",
  "peerDependencies": {
    "react-native": ">=0.52.0"
  },
  "dependencies": {
    "npm-registry": "^0.1.13",
    "semver": "^5.5.0",
    "valid-url": "^1.0.9"
  }
}
